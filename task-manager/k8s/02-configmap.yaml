apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: tasks-app
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "tasksdb"
  PORT:    "3000"
  NODE_ENV: "production"
